NEXT_PUBLIC_SUPABASE_URL=${NEXT_PUBLIC_SUPABASE_URL}
NEXT_PUBLIC_SUPABASE_ANON_KEY=${NEXT_PUBLIC_SUPABASE_ANON_KEY}
NEXT_PUBLIC_GOOGLE_MAPS_KEY=${NEXT_PUBLIC_GOOGLE_MAPS_KEY}
SUPABASE_EDGE_URL=${SUPABASE_EDGE_URL}

# Stripe
STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=${NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY}
STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}

# Supabase service role — Supabase Dashboard → Project Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}

# Stripe — IDs prix (créés le 04/05/2026)
STRIPE_PRICE_ASSO=${STRIPE_PRICE_ASSO}
STRIPE_PROMO_ASSO=${STRIPE_PROMO_ASSO}
STRIPE_PRICE_ESSENTIEL=${STRIPE_PRICE_ESSENTIEL}
STRIPE_PRICE_PERFORMANCE=${STRIPE_PRICE_PERFORMANCE}
STRIPE_PRICE_PREMIUM=${STRIPE_PRICE_PREMIUM}
